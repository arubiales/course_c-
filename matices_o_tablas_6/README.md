# Matrices

La forma de declararlas es muy similar a un vector, al principio son el número de filas y después el número de columnas, con el tipo de dato deseado.

```
int matriz_vect[nfilas][ncolumnas];
```

sabiendo esto, ya podemos llamar elementos, ingresar elementos, etc.